extends layout

block content
    form(method='POST')
        input(type='text' id='inputText' name='text')
        input(type='submit')
    ul
        each elem in elems
            li
                | #{elem.id} 
                | #{elem.text}
                form(action='/delete/'+elem.id method='POST')
                    input(type='submit' value='Del')